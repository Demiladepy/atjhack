# Optional: local Postgres for development if not using Supabase.
# Primary database for the app is Supabase (cloud).
# Uncomment and use if you need a local DB for testing.

# version: "3.8"
# services:
#   postgres:
#     image: postgres:15-alpine
#     environment:
#       POSTGRES_USER: postgres
#       POSTGRES_PASSWORD: postgres
#       POSTGRES_DB: smb_bookkeeper
#     ports:
#       - "5432:5432"
#     volumes:
#       - pgdata:/var/lib/postgresql/data
# volumes:
#   pgdata: {}
